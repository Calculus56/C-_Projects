namespaces allow for abstraction and organization.
 It allows for us to have multiple entry points and specify the one we want to use in the .csproj by using
 <StartupObject>namespace.class_name</StartupObject>